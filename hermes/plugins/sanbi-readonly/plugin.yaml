name: sanbi-readonly
version: 4.0.0
description: Deterministic Sanbi routing with trusted-root project discovery and lifecycle controls
provides_tools:
  - sanbi_projects
  - sanbi_project_status
  - sanbi_ask_lead

provides_hooks:
  - pre_llm_call
